let length =  12;

console.log(Array(length))

// If you are given a number of how long the array is supposed to be,
// you can make Array(length) and it will turn into an array of that
// many empty spaces; i.e. length = 5, Array(length) = [ , , , , ]